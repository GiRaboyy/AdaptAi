# 🔧 Исправления для деплоя на Vercel

## 📊 Диагностика

### Обнаруженная архитектура
- **Фреймворк**: Express.js (сервер) + React + Vite (клиент)
- **Роутинг**: Wouter (клиент) + Express (сервер)
- **Сборщик**: Vite (клиент) + esbuild (сервер)
- **Вывод сборки**: `dist/index.cjs` (сервер) + `dist/public/` (статика)

### Причины 404

1. ❌ **Отсутствовал `vercel.json`**
   - Vercel пытался развернуть как статический SPA вместо Node.js сервера
   - Нужна конфигурация для Node.js runtime

2. ✅ **SPA routing уже работал**
   - Файл `server/static.ts` уже содержал правильный catch-all для `index.html`
   - Проблема была только в конфигурации Vercel

3. ⚠️ **Переменные окружения использовались правильно**
   - Уже используется префикс `VITE_` для клиентских переменных ✅
   - Но отсутствовала валидация и понятные ошибки

---

## ✅ Выполненные исправления

### 1. Создан `vercel.json`
```json
{
  "version": 2,
  "builds": [
    {
      "src": "dist/index.cjs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/healthz",
      "dest": "dist/index.cjs"
    },
    {
      "src": "/readyz",
      "dest": "dist/index.cjs"
    },
    {
      "src": "/api/(.*)",
      "dest": "dist/index.cjs"
    },
    {
      "src": "/(.*)",
      "dest": "dist/index.cjs"
    }
  ]
}
```

**Что делает:**
- Указывает Vercel использовать Node.js runtime для `dist/index.cjs`
- Направляет ВСЕ запросы на Express сервер
- Сервер сам обрабатывает API маршруты и отдает статику
- Для SPA используется catch-all в `server/static.ts` (уже был)

### 2. Создан `client/src/lib/env-validation.ts`
**Функционал:**
- Валидация обязательных переменных окружения
- Безопасные сообщения об ошибках (без утечки секретов)
- Хелперы для получения env с fallback
- Проверка окружения (dev/prod)

**Обязательные переменные:**
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

**Опциональные:**
- `VITE_APP_URL` (fallback на `window.location.origin`)

### 3. Обновлен `client/src/lib/supabase.ts`
**Изменения:**
- Использует `env-validation` для получения переменных
- Валидирует наличие переменных при инициализации
- Понятные сообщения об ошибках в консоли
- Логирует успешную инициализацию

### 4. Обновлен `client/src/hooks/use-auth.ts`
**Изменения:**
- Использует `getAppUrl()` вместо прямого `import.meta.env`
- Правильный fallback на `window.location.origin`

---

## 🎯 Что нужно сделать

### 1. Настройки Vercel Project Settings

#### Build Settings:
```
Framework Preset: Other
Build Command: npm run build
Output Directory: dist
Install Command: npm ci
Node.js Version: 20.x
```

### 2. Переменные окружения в Vercel

**Добавьте эти переменные в: Project Settings → Environment Variables**

Для всех окружений (Development, Preview, Production):

#### База данных (PostgreSQL):
```bash
DATABASE_URL=postgresql://postgres:password@host:5432/database?sslmode=require
```

#### Supabase (Backend - без VITE_):
```bash
DATABASE_FILE_STORAGE_URL=https://your-project-ref.supabase.co
DATABASE_FILE_STORAGE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-key
```

#### Supabase (Frontend - с VITE_):
```bash
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

#### URL приложения:
```bash
APP_URL=https://your-domain.vercel.app
VITE_APP_URL=https://your-domain.vercel.app
```
⚠️ **ВАЖНО**: Используйте реальный домен Vercel, НЕ localhost

#### Безопасность:
```bash
SESSION_SECRET=your-random-secret-32-chars-minimum
COOKIE_SECURE=true
```

#### AI (Yandex Cloud):
```bash
YANDEX_CLOUD_API_KEY=your-key
YANDEX_CLOUD_PROJECT_FOLDER_ID=your-folder-id
YANDEX_CLOUD_BASE_URL=https://rest-assistant.api.cloud.yandex.net/v1
YANDEX_PROMPT_ID=your-prompt-id
YANDEX_TIMEOUT_MS=90000
```

#### Email (SMTP):
```bash
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=your-email@yandex.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=Your App <your-email@yandex.com>
```

#### Система:
```bash
NODE_ENV=production
PORT=5000
HOST=0.0.0.0
LOG_LEVEL=info
LOG_PRETTY=false
SHUTDOWN_TIMEOUT_MS=10000
REQUEST_LOG_SAMPLE=0.1
```

### 3. Обновить Redirect URLs в Supabase

**Supabase Dashboard → Authentication → URL Configuration:**

Добавьте:
```
https://your-domain.vercel.app/auth/callback
```

---

## 🧪 Проверка после деплоя

### Критерии успешного деплоя:

1. ✅ Главная страница `/` открывается без 404
2. ✅ Прямые переходы работают:
   - `/auth`
   - `/app/courses`
   - `/curator`
   - `/curator/course/123`
3. ✅ Обновление страницы (F5) не показывает 404
4. ✅ Авторизация через Supabase работает
5. ✅ Подтверждение email работает (ссылки в письмах)
6. ✅ API запросы выполняются
7. ✅ Нет ошибок в консоли браузера о переменных окружения
8. ✅ В логах Vercel нет ошибок сборки

### Как проверить локально:

```bash
npm run build
npm run start
# Открыть http://localhost:5000
```

---

## 🔍 Частые проблемы

### Проблема: 404 на всех маршрутах
**Решение**: Проверьте что `vercel.json` существует и закоммичен в репозиторий

### Проблема: "Missing environment variables" в консоли
**Решение**: Добавьте все `VITE_*` переменные в Vercel Project Settings

### Проблема: Supabase auth не работает
**Решение**: 
1. Проверьте `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`
2. Убедитесь что значения совпадают с Supabase Dashboard → Settings → API
3. Проверьте что `APP_URL` указывает на ваш домен Vercel

### Проблема: Ссылки подтверждения email не работают
**Решение**:
1. Установите `APP_URL` и `VITE_APP_URL` на ваш production домен
2. Обновите redirect URLs в Supabase Dashboard
3. Убедитесь что НЕ используется localhost

---

## 📋 Изменённые файлы

### Новые файлы:
- ✅ `/vercel.json` - конфигурация Vercel
- ✅ `/client/src/lib/env-validation.ts` - валидация переменных
- ✅ `/VERCEL_DEPLOYMENT_GUIDE.md` - полная инструкция (английский)
- ✅ `/VERCEL_FIX_SUMMARY.md` - этот файл (русский)

### Изменённые файлы:
- ✅ `/client/src/lib/supabase.ts` - добавлена валидация env
- ✅ `/client/src/hooks/use-auth.ts` - использует хелперы env

### НЕ изменено:
- ❌ Дизайн - никаких изменений UI
- ❌ Роутинг - логика маршрутизации не менялась
- ❌ API - бэкенд логика не тронута
- ❌ Компоненты - React компоненты не изменены

---

## 📝 Краткий итог

### Основная проблема
Vercel пытался развернуть приложение как статический SPA, но это fullstack Express приложение.

### Решение
Добавлен `vercel.json` который указывает Vercel:
- Использовать Node.js runtime
- Направлять все запросы на Express сервер
- Сервер сам обрабатывает маршруты и отдает статику

### Дополнительно
- Добавлена валидация переменных окружения
- Улучшены сообщения об ошибках
- Создана документация

---

## 🚀 Следующие шаги

1. **Добавьте все переменные окружения в Vercel**
   - Project Settings → Environment Variables
   - Добавьте для Preview и Production

2. **Обновите Supabase redirect URLs**
   - Authentication → URL Configuration
   - Добавьте ваш Vercel домен

3. **Закоммитьте изменения**
   ```bash
   git add .
   git commit -m "Fix Vercel deployment configuration"
   git push
   ```

4. **Vercel автоматически задеплоит**
   - Проверьте логи сборки
   - Протестируйте все маршруты
   - Проверьте консоль браузера

---

**Всё готово к деплою! 🚀**

Подробная инструкция на английском: `/VERCEL_DEPLOYMENT_GUIDE.md`
