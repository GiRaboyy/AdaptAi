# 🚀 Развертывание на Vercel - Быстрый старт

## ⚡ Коротко

**Проблема:** 404 на всех страницах после деплоя на Vercel (локально работает)

**Причина:** Vercel думал что это статический сайт, а это fullstack Express приложение

**Решение:** Создан `vercel.json` + валидация переменных окружения

**Что нужно сделать:**
1. Добавить переменные окружения в Vercel
2. Обновить redirect URLs в Supabase
3. Запушить изменения в GitHub

---

## 📋 3 шага до деплоя

### Шаг 1: Настройки проекта Vercel

**Vercel Dashboard → Ваш проект → Settings**

```
Build Command:       npm run build
Output Directory:    dist
Install Command:     npm ci
Node.js Version:     20.x
```

### Шаг 2: Добавить переменные окружения

**Settings → Environment Variables**

Обязательные переменные (добавить для Preview и Production):

```bash
# База данных
DATABASE_URL=postgresql://postgres:password@host:5432/db?sslmode=require

# Supabase (Бэкенд)
DATABASE_FILE_STORAGE_URL=https://xxx.supabase.co
DATABASE_FILE_STORAGE_KEY=xxx_service_role_key
SUPABASE_ANON_KEY=xxx_anon_key

# Supabase (Фронтенд - ОБЯЗАТЕЛЕН префикс VITE_!)
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=xxx_anon_key

# URL приложения (используйте ваш домен Vercel!)
APP_URL=https://ваше-приложение.vercel.app
VITE_APP_URL=https://ваше-приложение.vercel.app

# Безопасность
SESSION_SECRET=случайная-строка-32-символа
COOKIE_SECURE=true

# Yandex Cloud AI
YANDEX_CLOUD_API_KEY=ваш-ключ
YANDEX_CLOUD_PROJECT_FOLDER_ID=ваш-folder-id
YANDEX_CLOUD_BASE_URL=https://rest-assistant.api.cloud.yandex.net/v1
YANDEX_PROMPT_ID=ваш-prompt-id
YANDEX_TIMEOUT_MS=90000

# Email (SMTP)
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=ваш-email@yandex.com
SMTP_PASSWORD=пароль-приложения
SMTP_FROM=Ваше Приложение <ваш-email@yandex.com>

# Система
NODE_ENV=production
PORT=5000
HOST=0.0.0.0
LOG_LEVEL=info
LOG_PRETTY=false
SHUTDOWN_TIMEOUT_MS=10000
REQUEST_LOG_SAMPLE=0.1
```

⚠️ **ВАЖНО:**
- Переменные для фронтенда ДОЛЖНЫ иметь префикс `VITE_`
- В `APP_URL` используйте ваш домен Vercel, НЕ localhost
- Добавьте переменные и для Preview, и для Production

Полный список: `.env.vercel.template`

### Шаг 3: Обновить Supabase и задеплоить

1. **Supabase Dashboard** → Authentication → URL Configuration
   - Добавьте redirect URL: `https://ваше-приложение.vercel.app/auth/callback`
   - Обновите Site URL: `https://ваше-приложение.vercel.app`

2. **Запушить в GitHub:**
   ```bash
   git add .
   git commit -m "Исправление деплоя на Vercel"
   git push
   ```

3. **Vercel автоматически** соберет и задеплоит приложение

---

## ✅ Проверка работоспособности

После деплоя проверьте:

| Тест | Ожидаемый результат |
|------|---------------------|
| Открыть `/` | ✅ Главная загружается |
| Открыть `/auth` | ✅ Страница авторизации |
| Открыть `/app/courses` | ✅ Страница курсов |
| Открыть `/curator` | ✅ Панель куратора |
| F5 на любой странице | ✅ Нет 404 |
| Вход в систему | ✅ Работает |
| Подтверждение email | ✅ Ссылка работает |
| Консоль браузера | ✅ Нет ошибок env |

---

## 🐛 Частые проблемы

### ❌ Все еще 404

**Проверьте:**
1. `vercel.json` закоммичен в Git?
2. Сборка на Vercel прошла успешно? (смотрите логи)
3. Файл `dist/index.cjs` создается при сборке?

**Решение:**
```bash
# Проверить vercel.json
ls -la vercel.json

# Проверить сборку локально
npm run build
ls -la dist/index.cjs
```

### ❌ "Missing environment variables"

**Проверьте:**
1. Добавили `VITE_SUPABASE_URL`?
2. Добавили `VITE_SUPABASE_ANON_KEY`?
3. Названия переменных правильные? (регистр важен)

**Решение:**
- Project Settings → Environment Variables
- Добавьте все переменные с префиксом `VITE_`
- Передеплойте (автоматически при следующем push)

### ❌ Не работает авторизация

**Проверьте:**
1. `VITE_SUPABASE_URL` совпадает с Supabase Dashboard?
2. `VITE_SUPABASE_ANON_KEY` совпадает с Supabase?
3. Redirect URLs в Supabase содержат ваш домен Vercel?

**Решение:**
- Supabase Dashboard → Settings → API
- Скопируйте точные значения URL и Anon Key
- Убедитесь что redirect URL = `https://ваш-домен.vercel.app/auth/callback`

### ❌ Не работают ссылки в письмах

**Проверьте:**
1. `APP_URL` = ваш домен Vercel (не localhost)?
2. `VITE_APP_URL` = то же самое?
3. Redirect URLs в Supabase обновлены?

**Решение:**
- Установите `APP_URL=https://ваш-домен.vercel.app`
- Установите `VITE_APP_URL=https://ваш-домен.vercel.app`
- НЕ используйте localhost в production переменных

---

## 📊 Что было изменено

### Созданные файлы ✨
```
✅ vercel.json                          ← Конфигурация Vercel
✅ client/src/lib/env-validation.ts    ← Валидация переменных
✅ VERCEL_DEPLOYMENT_GUIDE.md          ← Полная инструкция (EN)
✅ VERCEL_FIX_SUMMARY.md               ← Краткая сводка (RU)
✅ DEPLOYMENT_FIX_REPORT.md            ← Технический отчет
✅ DEPLOYMENT_CHECKLIST.md             ← Чеклист
✅ VERCEL_QUICK_START.md               ← Быстрый старт (EN)
✅ БЫСТРЫЙ_СТАРТ.md                    ← Этот файл
✅ .env.vercel.template                ← Шаблон переменных
```

### Обновленные файлы 🔄
```
🔄 client/src/lib/supabase.ts          ← Улучшенная валидация
🔄 client/src/hooks/use-auth.ts        ← Использует env helpers
```

### Без изменений ❌
```
❌ Дизайн / UI
❌ Компоненты
❌ Логика роутинга
❌ API endpoints
❌ Схема базы данных
```

---

## 📚 Подробная документация

- **Полная инструкция**: `VERCEL_DEPLOYMENT_GUIDE.md` (English, подробно)
- **Краткая сводка**: `VERCEL_FIX_SUMMARY.md` (Русский, кратко)
- **Технический отчет**: `DEPLOYMENT_FIX_REPORT.md` (детальный анализ)
- **Чеклист**: `DEPLOYMENT_CHECKLIST.md` (пошаговые действия)
- **Шаблон env**: `.env.vercel.template` (все переменные)

---

## 🔍 Как работает решение

### Было:
```
Vercel → Пытается отдать статические файлы из dist/
       → Не находит index.html для /app/courses
       → 404 Page Not Found
```

### Стало:
```
Vercel → Запускает Node.js сервер (dist/index.cjs)
       → Express обрабатывает все маршруты
       → API routes → обработка на сервере
       → Остальное → статические файлы из dist/public/
       → Нет файла? → index.html (SPA fallback)
       → ✅ Все работает
```

### Что делает vercel.json:
```json
{
  "builds": [
    { "src": "dist/index.cjs", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "dist/index.cjs" }
  ]
}
```

**Переводя на русский:**
1. Используй Node.js для запуска `dist/index.cjs`
2. Направляй ВСЕ запросы на этот сервер
3. Сервер сам решит что отдать (API или статику)

---

## 🎯 Переменные окружения

### Правило префикса VITE_

**БЕЗ VITE_** = только сервер (секреты):
```bash
DATABASE_URL                    ← PostgreSQL connection
DATABASE_FILE_STORAGE_KEY       ← Service role key (секрет!)
SESSION_SECRET                  ← Ключ сессий
SMTP_PASSWORD                   ← Пароль email
```

**С VITE_** = доступно в браузере (публичное):
```bash
VITE_SUPABASE_URL              ← URL Supabase (публичный)
VITE_SUPABASE_ANON_KEY         ← Anon key (публичный, защищен RLS)
VITE_APP_URL                   ← URL приложения
```

### Почему так?

Vite (сборщик фронтенда) умеет читать только переменные с префиксом `VITE_`:

```typescript
// ✅ РАБОТАЕТ в браузере
const url = import.meta.env.VITE_SUPABASE_URL;

// ❌ НЕ РАБОТАЕТ в браузере (undefined)
const url = process.env.SUPABASE_URL;

// ✅ РАБОТАЕТ на сервере
const secret = process.env.DATABASE_FILE_STORAGE_KEY;
```

---

## 🔐 Безопасность

### ⚠️ НИКОГДА не добавляйте VITE_ к секретам:

**НЕ ДЕЛАЙТЕ ТАК:**
```bash
❌ VITE_DATABASE_FILE_STORAGE_KEY=service_role_key
❌ VITE_SESSION_SECRET=secret
❌ VITE_SMTP_PASSWORD=password
```

Эти переменные попадут в браузер и будут видны всем!

**ПРАВИЛЬНО:**
```bash
✅ DATABASE_FILE_STORAGE_KEY=service_role_key  (только сервер)
✅ SESSION_SECRET=secret                       (только сервер)
✅ SMTP_PASSWORD=password                      (только сервер)
```

### ✅ Можно добавлять VITE_ к публичным данным:

```bash
✅ VITE_SUPABASE_URL=https://xxx.supabase.co
✅ VITE_SUPABASE_ANON_KEY=anon_key  (защищен RLS в Supabase)
✅ VITE_APP_URL=https://your-app.vercel.app
```

---

## 🎉 Готово к деплою!

### Ваши действия:

1. ✅ Исправления применены
2. ⏳ Добавить переменные в Vercel
3. ⏳ Обновить Supabase redirect URLs
4. ⏳ Запушить в GitHub
5. ✅ Vercel автоматически задеплоит

### Ожидаемый результат:

- ✅ Все страницы открываются
- ✅ Нет 404 ошибок
- ✅ Авторизация работает
- ✅ Подтверждение email работает
- ✅ Нет ошибок в консоли

---

**Вопросы?** Смотрите детальные инструкции в других файлах документации.

**Статус:** 🟢 Готово к развертыванию
