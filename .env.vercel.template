# ===============================================
# VERCEL ENVIRONMENT VARIABLES TEMPLATE
# ===============================================
# Copy these to Vercel Project Settings → Environment Variables
# Set for both Preview and Production environments
# ===============================================

# ===== DATABASE =====
# PostgreSQL connection string from Supabase
# Get from: Supabase Dashboard → Settings → Database → Connection string (URI)
DATABASE_URL=postgresql://postgres:password@host:5432/database?sslmode=require

# ===== SUPABASE (BACKEND) =====
# Server-side Supabase configuration
# Get from: Supabase Dashboard → Settings → API
DATABASE_FILE_STORAGE_URL=https://your-project-ref.supabase.co
DATABASE_FILE_STORAGE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-key

# ===== SUPABASE (FRONTEND) =====
# IMPORTANT: Must use VITE_ prefix for client-side access
# These are the same values as above but exposed to the browser
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# ===== APPLICATION URLs =====
# CRITICAL: Use your actual Vercel domain, NOT localhost
# Used for email verification links and OAuth redirects
APP_URL=https://your-domain.vercel.app
VITE_APP_URL=https://your-domain.vercel.app

# ===== SECURITY =====
# Generate a secure random string (32+ characters)
SESSION_SECRET=your-random-secret-here-change-in-production
COOKIE_SECURE=true

# ===== AI INTEGRATION (YANDEX CLOUD) =====
YANDEX_CLOUD_API_KEY=your-yandex-api-key
YANDEX_CLOUD_PROJECT_FOLDER_ID=your-yandex-folder-id
YANDEX_CLOUD_BASE_URL=https://rest-assistant.api.cloud.yandex.net/v1
YANDEX_PROMPT_ID=your-yandex-prompt-id
YANDEX_TIMEOUT_MS=90000

# ===== EMAIL (SMTP) =====
# For Yandex SMTP, generate APP PASSWORD at:
# https://id.yandex.ru/security → App passwords
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=your-email@yandex.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=Your App <your-email@yandex.com>

# ===== APPLICATION CONFIGURATION =====
NODE_ENV=production
PORT=5000
HOST=0.0.0.0
LOG_LEVEL=info
LOG_PRETTY=false
SHUTDOWN_TIMEOUT_MS=10000
REQUEST_LOG_SAMPLE=0.1

# ===== OWNER CONTACT (OPTIONAL) =====
OWNER_TELEGRAM=@YourTelegramHandle

# ===============================================
# NOTES:
# ===============================================
# 
# VITE_ PREFIX:
# - Variables with VITE_ prefix are exposed to the browser
# - NEVER put secrets in VITE_ variables (visible in client code)
# - Only use VITE_ for public configuration (URLs, public keys)
#
# SERVICE ROLE KEY:
# - DATABASE_FILE_STORAGE_KEY is the service role key
# - NEVER expose this to the client (server-side only)
# - Has admin access to Supabase
#
# ANON KEY:
# - SUPABASE_ANON_KEY and VITE_SUPABASE_ANON_KEY are the same value
# - This is the public anon key (safe to expose to client)
# - Used for authentication and RLS
#
# APP URLs:
# - Must point to your production Vercel domain
# - Used in email verification links
# - Update Supabase Dashboard → Authentication → URL Configuration
#   with: https://your-domain.vercel.app/auth/callback
#
# ===============================================
